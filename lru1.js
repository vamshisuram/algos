
class LRUCache {
    cache = new Map();
    // array would create issue in copying and moving

    get(k) {
        // delete 
        // append add
    }

    delete(k) {
        // remove k
    }

    set(k, v) {
        // set k, v
    }

    getLRU() {
        // since we cannot access without any key.. need to convert to array
        // pick the 0th item in array because that's the oldest
    }

    getMRC() {
        // since we cannot access via position on the index
        // need to convert to array, pick the last
    }
}



